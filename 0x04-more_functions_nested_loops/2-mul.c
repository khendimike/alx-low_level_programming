#include "main.h"
/**
* mul - Multiplies two numbers.
* @a: first argument
* @b: second argument multiplied with the first.
*
* Return: product of two integers.
*/

int mul(int a, int b)
{
return (a * b);
}
